import { PackingListUpload } from "@/components/packing-list-upload";

export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background p-6">
      <PackingListUpload />
    </main>
  );
}
